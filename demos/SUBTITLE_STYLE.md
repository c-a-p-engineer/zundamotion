# デモ用字幕ルール

横長デモ（640×360）は、`font_size: 30`、`max_chars_per_line: 18`、下部配置
`y: h-38-text_h/2` を標準にする。背景ボックスは半透明・最小余白に留め、映像と
キャラクターを覆いすぎないようにする。縦型（360×640）は `font_size: 24` と
`max_chars_per_line: 13` を使う。変更時は動画をPNG化し、横切れ・視認性・重なりを確認する。
