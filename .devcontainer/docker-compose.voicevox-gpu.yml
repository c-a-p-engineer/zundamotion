services:
  voicevox:
    image: voicevox/voicevox_engine:nvidia-ubuntu24.04-latest
    runtime: nvidia
    gpus: all
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
